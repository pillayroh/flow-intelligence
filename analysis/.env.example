# Copy to .env and fill in. .env is gitignored — never commit it.
# Project URL (same one baked into the extension config).
SUPABASE_URL=https://uluhtqttiuuatxxgahre.supabase.co

# Service-role key: Supabase dashboard -> Project Settings -> API -> service_role.
# This key bypasses RLS and can read every table. Keep it local only.
SUPABASE_SERVICE_ROLE_KEY=